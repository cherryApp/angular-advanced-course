import { SimpleFilterPipe } from './simple-filter.pipe';

describe('SimpleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SimpleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
